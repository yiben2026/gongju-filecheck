services:
  - type: web
    name: gongju-filecheck
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: '20'
    autoDeploy: true
